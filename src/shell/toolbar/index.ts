/**
 * Minimal top bar.
 *
 * Built in phase 02 with its baseline controls plus the plugin contribution
 * point; consolidated in phase 08
 * (`prompts/08-visual-language-and-consolidation.md`), which decides which
 * controls stay visible and which become keybind-only.
 */

export { Toolbar } from "./Toolbar";
