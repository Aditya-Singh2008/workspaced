/**
 * Sidebar shell. Added in phase 02 so the toolbar's sidebar toggle has a
 * target; filled in by phases 03 and 06 through the plugin contract.
 */

export { Sidebar } from "./Sidebar";
export type { SidebarPanelId } from "./panels";
